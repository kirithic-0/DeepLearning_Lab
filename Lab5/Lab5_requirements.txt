# TensorFlow's native-Windows GPU support tops out at 2.10, which needs Python 3.10.
# On Linux / WSL2 / Mac, drop this upper bound and just use `pip install tensorflow`.
tensorflow<2.11
tensorflow-datasets<4.9
numpy<1.24
pandas>=1.5.0
matplotlib>=3.5.0
seaborn>=0.12.0
scikit-learn>=1.0.0
jupyter>=1.0.0
ipykernel>=6.0.0

# If TensorFlow raises a protobuf-related import error after installing the above:
# pip install "protobuf==3.19.6"
